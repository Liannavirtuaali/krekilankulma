---
phase: 01-perusta-tietokantarakenne
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/pages/.gitkeep
  - public/admin/.gitkeep
  - public/assets/css/style.css
  - public/assets/js/.gitkeep
  - public/assets/img/.gitkeep
  - public/uploads/.gitkeep
  - public/src/includes/config.php
  - public/src/includes/header.php
  - public/src/includes/footer.php
  - public/src/includes/nav.php
  - public/src/includes/helpers.php
  - public/.htaccess
  - public/src/includes/.htaccess
autonomous: true
requirements:
  - DB-01
  - DB-04
  - DB-05

must_haves:
  truths:
    - "Hakemistorakenne on luotu: public/pages/, public/admin/, public/assets/, public/uploads/, public/src/includes/"
    - "PHP include -rakenne toimii: header.php, footer.php, nav.php ovat olemassa ja sisältävät oikean rakenteen"
    - "config.php on suojattu: .htaccess estää suoran HTTP-latauksen"
    - "$page_title-muuttuja toimii: header.php käyttää sitä <title>-tagissa"
  artifacts:
    - "public/src/includes/config.php — define()-vakiot DB-tunnisteineen"
    - "public/src/includes/db.php — PDO-yhteysfunktio (luodaan Plan 02:ssa)"
    - "public/src/includes/header.php — HTML-rakenne, $page_title-tuki, nav.php include"
    - "public/src/includes/footer.php — HTML-rakenne, copyright"
    - "public/src/includes/nav.php — navigaatio kaikilla sivulinkeillä"
    - "public/.htaccess — Deny from all config.php:lle"
  key_links:
    - "header.php sisältää nav.php:n require_once:lla"
    - "Kaikki PHP-sivut käyttävät header.php + footer.php include-ketjua"
---

<objective>
Luo virtuaalitallin hakemistorakenne ja PHP include -sivupohja.

Purpose: Tämä plan luo koko projektin hakemistopuun ja sivupohjakomponentit. Kaikki muut planit ja tulevat vaiheet rakentuvat tähän perustaan.
Output: Täydellinen hakemistorakenne, header.php, footer.php, nav.php, config.php, helpers.php ja .htaccess-suojaus.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Luo hakemistorakenne</name>
  <files>public/pages/.gitkeep, public/admin/.gitkeep, public/assets/css/.gitkeep, public/assets/js/.gitkeep, public/assets/img/.gitkeep, public/uploads/.gitkeep, public/src/includes/.gitkeep</files>
  <read_first>.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md (D-01, D-02, D-03)</read_first>
  <action>
Luo seuraava hakemistorakenne projektijuureen (kaikki Altervistan public/-juureen):

```
public/
  pages/              ← julkiset PHP-sivut (Phase 2:ssa)
  admin/              ← admin-paneeli (Phase 3:ssa)
  assets/
    css/
    js/
    img/
  uploads/            ← hevosten kuvat (Phase 3:ssa)
  src/
    includes/         ← PHP-komponentit
```

Lisää .gitkeep tyhjiin kansioihin jotta ne versioituvat. Luo myös README.txt uploads/-kansioon: "Tämä hakemisto on hevosten kuville. Älä poista. / This directory is for horse images. Do not delete."
  </action>
  <verify>ls -la public/ && ls -la public/src/includes/</verify>
  <acceptance_criteria>
    - "public/pages/ on olemassa"
    - "public/admin/ on olemassa"
    - "public/uploads/ on olemassa"
    - "public/src/includes/ on olemassa"
  </acceptance_criteria>
  <done>Kaikki hakemistot luotu</done>
</task>

<task type="auto">
  <name>Task 2: Luo config.php (DB-tunnisteet define()-vakioina)</name>
  <files>public/src/includes/config.php</files>
  <read_first>.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md (D-15, D-16, D-17)</read_first>
  <action>
Luo public/src/includes/config.php seuraavalla sisällöllä:

```php
<?php
/**
 * Sovelluksen konfiguraatio
 * TÄRKEÄÄ: Tämä tiedosto on suojattu .htaccess:lla. Älä siirrä tätä pois src/includes/-kansiosta.
 */

// Sivuston asetukset
define('SITE_NAME', 'Virtuaalitalli');
define('SITE_URL', 'https://your-domain.altervista.org'); // Päivitä tähän oikea domain

// Uploads-polku
define('UPLOADS_DIR', __DIR__ . '/../../uploads/');
define('UPLOADS_URL', SITE_URL . '/uploads/');

// Kuvan latauksen asetukset
define('MAX_PHOTOS_PER_HORSE', 5);
define('MAX_UPLOAD_SIZE', 5 * 1024 * 1024); // 5 MB
define('ALLOWED_MIME_TYPES', ['image/jpeg', 'image/png', 'image/gif', 'image/webp']);
define('ALLOWED_EXTENSIONS', ['jpg', 'jpeg', 'png', 'gif', 'webp']);

// Session-asetukset
define('SESSION_NAME', 'vt_session');
define('SESSION_LIFETIME', 3600); // 1 tunti
```

HUOM: DB-tunnisteet ovat db.php:ssä (Plan 02), ei tässä tiedostossa.
  </action>
  <verify>php -l public/src/includes/config.php</verify>
  <acceptance_criteria>
    - "config.php sisältää 'define('"
    - "config.php sisältää 'SITE_NAME'"
    - "config.php sisältää 'MAX_PHOTOS_PER_HORSE'"
  </acceptance_criteria>
  <done>config.php luotu ilman syntaksivirheitä</done>
</task>

<task type="auto">
  <name>Task 3: Luo nav.php (navigaatio)</name>
  <files>public/src/includes/nav.php</files>
  <read_first>.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md (D-18, D-19, D-20)</read_first>
  <action>
Luo public/src/includes/nav.php:

```php
<?php
/**
 * Navigaatio — sisällytetään header.php:n kautta
 * Käytetään PHP_SELF:iä aktiivisen linkin tunnistamiseen
 */
$current_page = basename($_SERVER['PHP_SELF']);
?>
<nav>
  <ul>
    <li><a href="<?= SITE_URL ?>/pages/index.php"<?= $current_page === 'index.php' ? ' class="active"' : '' ?>>Etusivu</a></li>
    <li><a href="<?= SITE_URL ?>/pages/hevoset.php"<?= $current_page === 'hevoset.php' ? ' class="active"' : '' ?>>Hevoset</a></li>
    <li><a href="<?= SITE_URL ?>/pages/kasvatus.php"<?= $current_page === 'kasvatus.php' ? ' class="active"' : '' ?>>Kasvatus</a></li>
    <li><a href="<?= SITE_URL ?>/pages/yhteystiedot.php"<?= $current_page === 'yhteystiedot.php' ? ' class="active"' : '' ?>>Yhteystiedot</a></li>
  </ul>
</nav>
```
  </action>
  <verify>php -l public/src/includes/nav.php</verify>
  <acceptance_criteria>
    - "nav.php sisältää 'Etusivu'"
    - "nav.php sisältää 'Hevoset'"
    - "nav.php sisältää 'class=\"active\"'"
  </acceptance_criteria>
  <done>nav.php luotu</done>
</task>

<task type="auto">
  <name>Task 4: Luo header.php (sivupohja)</name>
  <files>public/src/includes/header.php</files>
  <read_first>.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md (D-18, D-19, D-20)</read_first>
  <action>
Luo public/src/includes/header.php:

```php
<?php
/**
 * Sivupohjan yläosa — sisällytetään joka sivulla
 * 
 * Vaatii ennen include:a:
 *   $page_title = 'Sivun otsikko';
 *   require_once __DIR__ . '/../src/includes/header.php';
 */

// Varmista että config on ladattu
if (!defined('SITE_NAME')) {
    require_once __DIR__ . '/config.php';
}

// XSS-suojaus: sanitoi $page_title
$page_title = isset($page_title) ? htmlspecialchars($page_title, ENT_QUOTES, 'UTF-8') : SITE_NAME;
?>
<!DOCTYPE html>
<html lang="fi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><?= $page_title ?> — <?= SITE_NAME ?></title>
  <link rel="stylesheet" href="<?= SITE_URL ?>/assets/css/style.css">
</head>
<body>
<header>
  <div class="site-header">
    <a href="<?= SITE_URL ?>/pages/index.php" class="site-title"><?= SITE_NAME ?></a>
  </div>
  <?php require_once __DIR__ . '/nav.php'; ?>
</header>
<main>
```
  </action>
  <verify>php -l public/src/includes/header.php</verify>
  <acceptance_criteria>
    - "header.php sisältää 'require_once __DIR__ . '/nav.php''"
    - "header.php sisältää 'htmlspecialchars'"
    - "header.php sisältää '$page_title'"
  </acceptance_criteria>
  <done>header.php luotu</done>
</task>

<task type="auto">
  <name>Task 5: Luo footer.php (sivupohja)</name>
  <files>public/src/includes/footer.php</files>
  <read_first>.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md (D-18)</read_first>
  <action>
Luo public/src/includes/footer.php:

```php
</main>
<footer>
  <div class="site-footer">
    <p>&copy; <?= date('Y') ?> <?= SITE_NAME ?>. Kaikki oikeudet pidätetään.</p>
  </div>
</footer>
</body>
</html>
```
  </action>
  <verify>php -l public/src/includes/footer.php</verify>
  <acceptance_criteria>
    - "footer.php sisältää '</footer>'"
    - "footer.php sisältää 'date('"
  </acceptance_criteria>
  <done>footer.php luotu</done>
</task>

<task type="auto">
  <name>Task 6: Luo helpers.php (apufunktiot)</name>
  <files>public/src/includes/helpers.php</files>
  <read_first>.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md (Discretion)</read_first>
  <action>
Luo public/src/includes/helpers.php apufunktioilla joita kaikki vaiheet tarvitsevat:

```php
<?php
/**
 * Apufunktiot — sisällytetään db.php:n kautta automaattisesti
 */

/**
 * Sanitoi käyttäjäsyöte HTML-tulostusta varten (XSS-suojaus)
 */
function e(string $value): string {
    return htmlspecialchars($value, ENT_QUOTES, 'UTF-8');
}

/**
 * Sanitoi käyttäjäsyöte — poistaa ylimääräiset välilyönnit
 */
function sanitize(string $value): string {
    return trim(strip_tags($value));
}

/**
 * Ohjaa käyttäjä toiselle sivulle ja pysäyttää skriptin
 */
function redirect(string $url): never {
    header('Location: ' . $url);
    exit;
}

/**
 * Tarkistaa onko admin kirjautunut sisään
 */
function isLoggedIn(): bool {
    return isset($_SESSION['admin_logged_in']) && $_SESSION['admin_logged_in'] === true;
}

/**
 * Vaatii admin-kirjautumista — ohjaa login-sivulle jos ei kirjautunut
 */
function requireLogin(): void {
    if (!isLoggedIn()) {
        redirect(SITE_URL . '/admin/login.php');
    }
}

/**
 * Muotoilee päivämäärän suomalaiseen muotoon
 */
function formatDate(?string $date): string {
    if (!$date) return '—';
    return date('d.m.Y', strtotime($date));
}

/**
 * Laskee hevosen iän syntymäpäivästä
 */
function calculateAge(?string $birthDate): ?int {
    if (!$birthDate) return null;
    $birth = new DateTime($birthDate);
    $now = new DateTime();
    return (int) $now->diff($birth)->y;
}
```
  </action>
  <verify>php -l public/src/includes/helpers.php</verify>
  <acceptance_criteria>
    - "helpers.php sisältää 'function e('"
    - "helpers.php sisältää 'function isLoggedIn'"
    - "helpers.php sisältää 'function requireLogin'"
    - "helpers.php sisältää 'function calculateAge'"
  </acceptance_criteria>
  <done>helpers.php luotu kaikkine apufunktioineen</done>
</task>

<task type="auto">
  <name>Task 7: Luo .htaccess (konfiguraatiosuojaus + URL-rakenne)</name>
  <files>public/.htaccess, public/src/includes/.htaccess</files>
  <read_first>.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md (D-15)</read_first>
  <action>
Luo public/.htaccess Altervista-yhteensopivilla säännöillä:

```apache
# Altervista virtuaalitalli — pääkonfiguraatio
Options -Indexes

# Estä suora pääsy src/-kansioon
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^src/ - [F,L]
</IfModule>

# PHP-virheiden piilotus tuotannossa (tietoturva)
php_flag display_errors Off
php_flag log_errors On
```

Luo myös public/src/includes/.htaccess:

```apache
# Estä suora HTTP-pääsy konfiguraatiotiedostoihin
<Files "config.php">
    Order Deny,Allow
    Deny from all
</Files>

<Files "db.php">
    Order Deny,Allow
    Deny from all
</Files>
```

HUOM: Altervistassa .htaccess-tuki vaihtelee. Jos `php_flag` ei toimi (500-virhe), poista nuo rivit. `Deny from all` pitäisi toimia kaikissa tavallisissa Apache-kokoonpanoissa.
  </action>
  <verify>cat public/src/includes/.htaccess</verify>
  <acceptance_criteria>
    - "public/src/includes/.htaccess sisältää 'Deny from all'"
    - "public/src/includes/.htaccess sisältää 'config.php'"
    - "public/src/includes/.htaccess sisältää 'db.php'"
  </acceptance_criteria>
  <done>.htaccess-tiedostot luotu</done>
</task>

<task type="auto">
  <name>Task 8: Luo assets/css/style.css (perustyylit)</name>
  <files>public/assets/css/style.css</files>
  <action>
Luo public/assets/css/style.css minimaalinen CSS-pohja jolla sivusto on käytettävä (täydellinen tyyli tehdään Phase 2:ssa tai erikseen):

```css
/* Virtuaalitalli — perustyyli */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: Georgia, 'Times New Roman', serif;
  background: #f9f7f4;
  color: #2c2c2c;
  line-height: 1.6;
}

header {
  background: #3d2b1f;
  color: #f9f7f4;
  padding: 1rem 2rem;
  display: flex;
  align-items: center;
  gap: 2rem;
  flex-wrap: wrap;
}

.site-title {
  color: #e8d5b7;
  text-decoration: none;
  font-size: 1.5rem;
  font-weight: bold;
}

nav ul {
  list-style: none;
  display: flex;
  gap: 1.5rem;
}

nav a {
  color: #c9b89a;
  text-decoration: none;
  font-size: 0.95rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

nav a:hover, nav a.active {
  color: #e8d5b7;
  text-decoration: underline;
}

main {
  max-width: 1100px;
  margin: 2rem auto;
  padding: 0 1.5rem;
}

footer {
  background: #3d2b1f;
  color: #c9b89a;
  text-align: center;
  padding: 1rem;
  margin-top: 3rem;
  font-size: 0.85rem;
}
```
  </action>
  <verify>test -f public/assets/css/style.css && echo "OK"</verify>
  <acceptance_criteria>
    - "style.css on olemassa"
    - "style.css sisältää 'body'"
    - "style.css sisältää 'nav'"
  </acceptance_criteria>
  <done>style.css luotu</done>
</task>

</tasks>

<verification>
Ennen suunnitelman valmiiksi merkitsemistä:
- [ ] php -l public/src/includes/config.php (ei syntaksivirheitä)
- [ ] php -l public/src/includes/header.php (ei syntaksivirheitä)
- [ ] php -l public/src/includes/footer.php (ei syntaksivirheitä)
- [ ] php -l public/src/includes/nav.php (ei syntaksivirheitä)
- [ ] php -l public/src/includes/helpers.php (ei syntaksivirheitä)
- [ ] cat public/src/includes/.htaccess | grep "Deny from all"
- [ ] ls public/pages/ public/admin/ public/uploads/ public/src/includes/
</verification>

<success_criteria>
- Kaikki hakemistot luotu oikeaan rakenteeseen
- header.php, footer.php, nav.php toimivat PHP include -ketjuna
- config.php ja db.php suojattu .htaccess:lla
- $page_title-muuttuja toimii header.php:ssa
- Ei PHP-syntaksivirheitä yhdessäkään tiedostossa
- style.css tarjoaa perustyylin
</success_criteria>

<output>
Luo .planning/phases/01-perusta-tietokantarakenne/01-01-SUMMARY.md kun kaikki tehtävät on suoritettu.
</output>
