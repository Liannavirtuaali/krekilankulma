---
phase: 01-perusta-tietokantarakenne
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - public/pages/index.php
  - public/index.php
autonomous: false
requirements:
  - DB-05

must_haves:
  truths:
    - "Perustestisivu toimii: PHP include -ketju (db.php → helpers.php, header.php → nav.php, footer.php) toimii virheettömästi"
    - "PDO-yhteys tietokantaan toimii: getDB() palauttaa PDO-instanssin ilman virheitä"
    - "Sukutaulukyselyfunktio toimii: getHorsePedigree() hakee rekursiivisesti 3 sukupolvea"
    - "Testidata luettavissa: 3 hevosta voidaan hakea DB:stä"
  artifacts:
    - "public/pages/index.php — väliaikainen testisivu joka testaa koko infrastruktuurin"
    - "public/index.php — uudelleenohjaus public/pages/index.php:hen"
    - "public/src/includes/helpers.php — päivitetty getHorsePedigree()-funktiolla"
  key_links:
    - "public/pages/index.php käyttää db.php + header.php + footer.php include-ketjua"
    - "getHorsePedigree() tekee rekursiiviset kyselyt PDO prepared statements -menetelmällä"

user_setup:
  - "Luo tietokanta Altervistan cPanelissa tai phpMyAdminissa"
  - "Aja database/schema.sql phpMyAdminissa (Import-toiminto)"
  - "Aja database/seed.sql phpMyAdminissa testidatalle"
  - "Päivitä public/src/includes/db.php: DB_NAME, DB_USER, DB_PASS oikeiksi arvoiksi"
  - "Lataa kaikki tiedostot FTP:llä Altervistaan"
  - "Tarkista että public/ on Altervistasi web-juuri tai sen aliakansio"
---

<objective>
Testaa koko infrastruktuuri toimivaksi ja lisää rekursiivinen sukutaulufunktio.

Purpose: Tämä on integraatiotestausvaihe — varmistetaan että Plan 01 (hakemistorakenne + sivupohja) ja Plan 02 (tietokanta + PDO) toimivat yhdessä ennen kuin Phase 2 rakentaa julkiset sivut tämän päälle. Sisältää myös rekursiivisen sukutaulufunktion jota Phase 2 tarvitsee.
Output: Toimiva testisivu, uudelleenohjaus juuresta, ja getHorsePedigree()-apufunktio.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
@~/.copilot/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-perusta-tietokantarakenne/01-01-SUMMARY.md
@.planning/phases/01-perusta-tietokantarakenne/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lisää getHorsePedigree()-funktio helpers.php:hen</name>
  <files>public/src/includes/helpers.php</files>
  <read_first>
    .planning/phases/01-perusta-tietokantarakenne/01-CONTEXT.md (D-07, D-08, D-09),
    public/src/includes/helpers.php
  </read_first>
  <action>
Lisää helpers.php:hen rekursiivinen sukutaulufunktio. Funktio hakee PDO:ta käyttäen hevosen sukutaulun 3 sukupolveen (vanhemmat, isovanhemmat, isoisovanhemmat).

Lisää tämä koodi helpers.php:n loppuun (calculateAge()-funktion jälkeen):

```php

/**
 * Hakee hevosen sukutaulun rekursiivisesti (maks. 3 sukupolvea)
 * 
 * @param int $horseId Hevosen ID
 * @param int $depth Nykyinen syvyys (0 = aloitushevonen, 1 = vanhemmat jne.)
 * @param int $maxDepth Maksimisyvyys (3 = isoisovanhemmat)
 * @return array|null Hevosen tiedot sukutauluineen tai null
 */
function getHorsePedigree(int $horseId, int $depth = 0, int $maxDepth = 3): ?array {
    if ($depth > $maxDepth || $horseId <= 0) {
        return null;
    }
    
    $db = getDB();
    $stmt = $db->prepare(
        'SELECT id, name, call_name, breed, birth_date, gender, color, 
                sire_id, dam_id, evm, profile_url
         FROM horses 
         WHERE id = :id AND is_deleted = 0'
    );
    $stmt->execute([':id' => $horseId]);
    $horse = $stmt->fetch();
    
    if (!$horse) {
        return null;
    }
    
    // Rekursiivinen haku: vanhemmat, isovanhemmat, isoisovanhemmat
    if ($depth < $maxDepth) {
        $horse['sire'] = $horse['sire_id'] 
            ? getHorsePedigree((int)$horse['sire_id'], $depth + 1, $maxDepth) 
            : null;
        $horse['dam'] = $horse['dam_id'] 
            ? getHorsePedigree((int)$horse['dam_id'], $depth + 1, $maxDepth) 
            : null;
    } else {
        $horse['sire'] = null;
        $horse['dam'] = null;
    }
    
    return $horse;
}
```

TÄRKEÄÄ: Tämä funktio vaatii getDB():n — db.php on jo sisällyttänyt helpers.php:n, joten getDB() on saatavilla. Älä lisää require-lausekkeita helpers.php:hen (vältetään sirkularinen riippuvuus).
  </action>
  <verify>php -l public/src/includes/helpers.php && grep -c "function " public/src/includes/helpers.php</verify>
  <acceptance_criteria>
    - "helpers.php sisältää 'function getHorsePedigree'"
    - "helpers.php sisältää 'PDO prepared statements' (getHorsePedigree käyttää prepare())"
    - "php -l public/src/includes/helpers.php ei tuota virheitä"
  </acceptance_criteria>
  <done>getHorsePedigree()-funktio lisätty helpers.php:hen</done>
</task>

<task type="auto">
  <name>Task 2: Luo väliaikainen testisivu (public/pages/index.php)</name>
  <files>public/pages/index.php</files>
  <read_first>
    public/src/includes/header.php,
    public/src/includes/footer.php,
    public/src/includes/db.php
  </read_first>
  <action>
Luo public/pages/index.php väliaikaisena testisivuna joka testaa koko include-ketjun ja tietokantayhteyden. Phase 2 korvaa tämän oikealla etusivulla.

```php
<?php
/**
 * Etusivu — VÄLIAIKAINEN TESTISIVU (korvaa Phase 2:ssa)
 */
require_once __DIR__ . '/../src/includes/db.php';
require_once __DIR__ . '/../src/includes/config.php';

$page_title = 'Etusivu';
require_once __DIR__ . '/../src/includes/header.php';
?>

<section class="home-hero">
  <h1>Tervetuloa virtuaalitalliin!</h1>
  <p>Sivusto on rakenteilla. Palaa pian.</p>
  
  <?php
  // TESTITULOSTUS: Poistetaan Phase 2:ssa
  try {
      $db = getDB();
      $stmt = $db->query('SELECT COUNT(*) as total FROM horses WHERE is_deleted = 0');
      $result = $stmt->fetch();
      echo '<p style="color:green;">✓ Tietokantayhteys toimii. Hevosia: ' . (int)$result['total'] . '</p>';
  } catch (Exception $e) {
      echo '<p style="color:red;">✗ Tietokantayhteys epäonnistui. Tarkista db.php-asetukset.</p>';
  }
  ?>
</section>

<?php require_once __DIR__ . '/../src/includes/footer.php'; ?>
```
  </action>
  <verify>php -l public/pages/index.php</verify>
  <acceptance_criteria>
    - "public/pages/index.php on olemassa"
    - "php -l public/pages/index.php ei tuota virheitä"
    - "public/pages/index.php sisältää 'require_once'"
    - "public/pages/index.php sisältää 'header.php'"
    - "public/pages/index.php sisältää 'footer.php'"
  </acceptance_criteria>
  <done>Testisivu luotu</done>
</task>

<task type="auto">
  <name>Task 3: Luo uudelleenohjaus juuresta (public/index.php)</name>
  <files>public/index.php</files>
  <action>
Luo public/index.php joka ohjaa automaattisesti pages/index.php:hen. Tämä mahdollistaa domain.com/-osoitteen toimimisen.

```php
<?php
/**
 * Juuren uudelleenohjaus → pages/index.php
 */
require_once __DIR__ . '/src/includes/config.php';
header('Location: ' . SITE_URL . '/pages/index.php', true, 301);
exit;
```
  </action>
  <verify>php -l public/index.php</verify>
  <acceptance_criteria>
    - "public/index.php on olemassa"
    - "public/index.php sisältää 'header('"
    - "public/index.php sisältää '301'"
  </acceptance_criteria>
  <done>Uudelleenohjaus luotu</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Koko infrastruktuuri on nyt luotu:
    - Hakemistorakenne (public/pages/, public/src/includes/, public/uploads/ jne.)
    - PHP include -rakenne (header.php, nav.php, footer.php, config.php, db.php, helpers.php)
    - MySQL-skeema (database/schema.sql) + testidata (database/seed.sql)
    - Testisivu (public/pages/index.php)
  </what-built>
  <how-to-verify>
    1. Luo tietokanta Altervistan phpMyAdminissa
    2. Aja database/schema.sql (Import)
    3. Aja database/seed.sql (Import)
    4. Päivitä public/src/includes/db.php: DB_NAME, DB_USER, DB_PASS
    5. Lataa kaikki tiedostot FTP:llä Altervistaan
    6. Vieraile domain.altervista.org/ — pitäisi näkyä testisivu
    7. Tarkista vihreä "✓ Tietokantayhteys toimii" -viesti
    8. Varmista että navigaatio näkyy (Etusivu, Hevoset, Kasvatus, Yhteystiedot)
  </how-to-verify>
  <resume-signal>Kirjoita "ok" jos kaikki toimii, tai kuvaile ongelmat</resume-signal>
</task>

</tasks>

<verification>
Ennen suunnitelman valmiiksi merkitsemistä:
- [ ] php -l public/src/includes/helpers.php (getHorsePedigree() lisätty)
- [ ] php -l public/pages/index.php
- [ ] php -l public/index.php
- [ ] grep "getHorsePedigree" public/src/includes/helpers.php
- [ ] grep "prepare(" public/src/includes/helpers.php (käytetään prepared statements)
- [ ] Altervistassa testisivu näyttää vihreän tietokantayhteys-viestin (checkpoint)
</verification>

<success_criteria>
- PHP include -ketju toimii virheettömästi (db.php → helpers.php, header.php → nav.php, footer.php)
- PDO-yhteys tietokantaan toimii (testisivu näyttää hevosten lukumäärän)
- getHorsePedigree() funktio on helpers.php:ssä ja käyttää prepared statements -menetelmää
- Koko phase 1 UAT-kriteerit täyttyvät (ks. ROADMAP.md Phase 1 Success Criteria)
</success_criteria>

<output>
Luo .planning/phases/01-perusta-tietokantarakenne/01-03-SUMMARY.md kun kaikki tehtävät on suoritettu.
</output>
